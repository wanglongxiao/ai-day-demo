# Secrets & local config (never commit)
.env
.env.*
!.env.example

# Local deployment notes (cloud resource ids — never commit)
DEPLOYMENT.local.md

# Docs are not tracked in GitHub (contain API refs / demo assets)
docs/

# Tests are not tracked in GitHub
tests/

# Python
__pycache__/
*.pyc
.venv/
.python-version

# OS
.DS_Store

# Local runtime
*.log
